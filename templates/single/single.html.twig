{% extends 'header.html.twig' %}

{% block title %}Bien choisi{% endblock %}

{% block body %}
<div class="container">
    <div class="row p-5">
        <div class="col-9">
            <img src="{{ asset('assets/img/' ~ bien.imageName ) }}" class="w-75" style="border:5px blue solid; border-radius:20px">
            <p class="h4 font-weight-bold mt-2">{{ bien.nom }}</p>
        </div>
        <div class="col-3 jumbotron text-center p-3">
            <p class="h2 font-weight-bold mt-4">{{ bien.proprio}}</p>
            <p class="h4 font-weight-bold mt-4">Solde de points:</p>
            <a href="/pret/{{bien.nom}}"> <button class="btn btn-primary font-weight-bold mt-4">Réserver</button></a>
           
        </div>

        <div class="col-12 p-3 mt-3 border-bottom border-top">
            <h4>Description</h4>
            <p>{{ bien.description }}</p>
        </div>
    </div>
</div>
{% endblock %}
