{% extends 'header.html.twig' %}

{% block body %}
<main class="container">
    <section class="row">
        <div class="col-12">
            <h2>Votre Compte</h2>
        </div>

        <div class="col-12 m-1">
        <h4>Nombre de points :<br>{{ user.points }}</h4>
        </div>

        <div class="col-12 m-1">
        {% for bien in biens %}
        <p>{{bien.nom}}</p>
        <img src="{{ asset('assets/img/' ~ bien.imageName ) }}" class="img-fluid w-25" style="border:5px blue solid; border-radius:20px">
        
        <p>{{bien.Pret}}</p>
        {% endfor %}
        </div>

        <div class="col-12 m-1">
        {% for pret in prets %}
            {{pret.date_debut}}



        {% endfor %}
        </div>

        <div class="col-12 m-1">
        <a href="/session/Ajout"><h5 class="font-weight-bold">Déposer une annonce</h5></a>
        </div>


    </section>
</main>
{% endblock %}
