{% extends 'header.html.twig' %}

{% block title %}MyLoc{% endblock %}
{% block body %}
<main class="container">
    <session class="row">

        {% if app.user %}

            <div class="col-12 p-3 mt-3" >
                <a href="/session/Ajout"><h5 class="btn btnfond font-weight-bold">Déposer une annonce</h5></a>
            </div>

        {% endif %}


        <div class="col-12">
            <h2 class="font-weight-bold my-5">Annonces</h2>
        </div>

            {% for bien in biens %}

                    <div class="col-3">

                        <img src="{{ asset('assets/img/' ~ bien.imageName ) }}" class="img-fluid img">
                        <div class="carte p-2">

                            <a class="lien h5 font-weight-bold nom"href="/single/{{ bien.nom }}">{{ bien.nom }}</a> <span>-</span> <a href="/categorie/{{ bien.categorie }}" class="lien">{{ bien.categorie }}</a>

                            <p>{{bien.proprio}}</p>
                        </div>
                    </div>

            {% endfor %}

    </session>
</main>
{% endblock %}