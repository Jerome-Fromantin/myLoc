{% extends 'header.html.twig' %}

{% block body %}
<main class="container">

        <div class="w-50">
            {{form_start(form)}}

            {{form_row(form.nom, {'label' : 'Nom : '}) }}
            {{form_row(form.description, {'label' :  'Description : '}) }}
            {{form_row(form.categorie, {'label' : 'Catégorie : '}) }}
            {{form_row(form.imageFile, {
                'label' : 'Image : '
            })
            }}

            {{form_end(form)}}


        </div>

        <form class="col-12" action="" method="post">

                <div class="form-group w-50">
                    <label for="name">Nom :</label>
                    <input  name ="name" type="text" class="form-control" id="name">
                </div>

                <div class="form-group w-50">
                    <label for="description">Description :</label>
                    <textarea name="description" type="text" class="form-control" id="description">Entrez la description du produit.</textarea>
                </div>

                <div class="form-group w-50">
                    <label for="categories">Catégories :</label><br>
                    <select name="xategories">
                        <option value="jardinage">Jardinage</option>
                        <option value="bricolage">Bricolage</option>
                    </select>
                </div>
                {{form_start(form)}}
                {{form(form.imageFile, {
                'label' : 'Image : '})
                }}
                {{form_end(form)}}
                <button type="submit" class="btn btn-primary">Ajouter</button>

        </form>

    </main>



{% endblock %}
